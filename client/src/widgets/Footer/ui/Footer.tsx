import { Player } from '@/features/player'
import { Menu } from '@/widgets/Menu'

export const Footer = () => {
    return (
        <footer>
            <Player />
            <Menu />
        </footer>
    )
}
